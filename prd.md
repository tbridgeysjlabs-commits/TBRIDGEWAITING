# PRD — T-Bridge Waiting (티브리지 웨이팅)

> 본 문서는 **메뉴얼·운영 가이드 제작**을 위한 제품 요구사항/기능 명세서입니다.  
> 구현 코드베이스(`tbridgeWaiting`) 기준으로 정리했으며, UI 카피·화면명은 실제 제품과 동일하게 맞췄습니다.

---

## 1. 문서 정보

| 항목 | 내용 |
|------|------|
| 제품명 | T-Bridge Waiting (티브리지 웨이팅) |
| 제품 유형 | 멀티테넌트(시설사 단위) 대기열 관리 SaaS |
| 주요 채널 | 고객 키오스크/태블릿, 고객 모바일(완료 페이지), 시설 관리자 Web, 시스템 관리자 Web, 사이니지 |
| 문서 목적 | 사용자 메뉴얼, 운영 가이드, 교육 자료 작성의 기준 문서 |

---

## 2. 제품 개요

### 2.1 한 줄 정의
시설(테마파크·액티비티 등)에서 **대기 등록 → 호출 → 입장/취소**를 디지털로 운영하고, **카카오 알림톡·사이니지**로 고객·현장 스태프에게 상태를 전달하는 시스템입니다.

### 2.2 핵심 가치
- 키오스크에서 빠른 웨이팅 등록
- 고객 휴대폰으로 대기 순번·호출·미루기·취소 안내
- 시설 관리자 화면에서 실시간 대기 운영
- 사이니지로 입장 호출 현황 표시
- 알림톡 발송 비용(충전금) 기반 과금

### 2.3 테넌트 모델
- 시설사마다 고유 **`facilityCode`** 를 가집니다. (예: `demo-park`)
- 고객/사이니지/시설 관리자 URL에 `facilityCode`가 포함됩니다.
- 시스템 관리자는 전체 시설을 통합 관리합니다.

---

## 3. 사용자(Persona) 및 권한

| 역할 | 설명 | 주요 진입 URL |
|------|------|----------------|
| 고객 (키오스크) | 현장 태블릿/키오스크에서 대기 등록 | `/w/{facilityCode}` |
| 고객 (모바일) | 알림톡 링크로 대기 현황·미루기·취소 | `/w/{facilityCode}/complete/{waitingId}` |
| 사이니지 | 대형 화면 입장 호출 표시 (로그인 없음) | `/signage/{facilityCode}` |
| 시설 관리자 | 해당 시설 대기 운영·설정·알림톡 충전 | `/admin/{facilityCode}/login` |
| 시스템 관리자 | 시설사 계정·전역 내역·공지·충전/취소 | `/system-admin/login` |

---

## 4. 정보 구조(IA) / 화면 맵

### 4.1 고객
```
/w/{code}                 홈(전화번호 입력)
  └─ /party               인원 선택
  └─ /agreement           약관 동의
  └─ /registered          등록 완료(키오스크, 자동 홈 복귀)

/w/{code}/complete/{id}   웨이팅 관리(모바일)
  └─ /postpone            순서 미루기(선택형 정책일 때)
  └─ /cancel              취소 딥링크
```

### 4.2 시설 관리자
```
/admin/{code}/login
/admin/{code}/waiting     대기자 관리
/admin/{code}/history     대기자 내역
/admin/{code}/customers   고객 관리
/admin/{code}/billing     알림톡(집계·충전·재발송)
/admin/{code}/notices     공지사항(열람)
/admin/{code}/settings    설정
```

### 4.3 시스템 관리자
```
/system-admin/login
/system-admin/facilities  시설사 관리
/system-admin/notices     공지사항 관리
/system-admin/history     대기자 내역(전체)
/system-admin/customers   고객 관리(전체)
/system-admin/billing     알림톡(집계·충전·취소)
/system-admin/settings    설정
```

### 4.4 사이니지
```
/signage/{code}
```

---

## 5. 핵심 사용자 시나리오

### 5.1 대기 등록 (키오스크)
1. 홈에서 휴대폰 번호 입력 → **대기 시작**
2. 입장 인원(웨이팅 유형별) 선택 — 합계 1명 이상
3. 이용약관·개인정보(필수), 마케팅(선택) 동의
4. 등록 완료 → 대기번호 표시 → 약 3초 후 홈으로 복귀
5. 동시에 카카오 알림톡(등록 완료) 발송 시도, 완료 페이지 링크 포함

**메뉴얼 포인트**
- 약관 필수 미동의 시 등록 불가
- 알림톡 잔액 부족 시에도 **웨이팅 등록 자체는 완료**될 수 있으며, 알림톡만 실패할 수 있음

### 5.2 고객 모바일 — 웨이팅 관리
1. 알림톡 링크로 완료 페이지 진입
2. 현재 대기 순번·호출 상태·매장 안내 확인
3. 정책에 따라 **미루기** / **대기 취소** 가능
4. 호출되면 입장 대기 시간(카운트다운) 안내

### 5.3 시설 관리자 — 호출 / 입장
1. **대기자 관리**에서 `대기 중` 목록 확인
2. **[호출]**  
   - 고객에게 입장 안내 알림톡 발송  
   - 사이니지 「입장 호출」 영역에 즉시 반영  
   - 입장 대기 시간(설정값, 기본 5분) 타이머 시작
3. **[입장하기]**  
   - 해당 팀을 대기 완료로 이동  
   - 호출용 알림톡·사이니지 호출 표시는 하지 않음
4. **[대기 취소]**  
   - 관리자 취소 처리 + 취소 안내 알림톡

### 5.4 미입장(타임아웃) 자동 처리
- 호출 후 **입장 대기 시간**이 지나면 시스템이 `미입장(no_show)`으로 자동 취소
- 고객에게 미입장 취소 안내 알림톡 발송
- 보드/사이니지/완료 페이지 조회·실시간 이벤트 시점에 만료 처리가 반영됨

### 5.5 미루기
시설 설정 `미루기 정책`에 따름.

| 정책 값 | 동작 |
|---------|------|
| 사용 안 함 (`none`) | 미루기 불가 |
| 맨 뒤로 (`last_position`) | 현재 대기열 맨 뒤로 이동 |
| 순서 선택 (`select_position`) | 현재보다 뒤의 팀 뒤를 선택해 이동 |

- 허용 횟수: 설정 `postponeLimit` (기본 3)
- pending 상태만 가능
- 성공 시 순서 변경 완료 알림톡 발송

---

## 6. 화면별 기능 명세 (메뉴얼 목차용)

### 6.1 시설 관리자

#### 6.1.1 대기자 관리
- 탭: 대기 중 / 대기 완료 / 대기 취소
- 행 정보: 순번, 등록번호, 인원, 등록 시각, 대기 시간
- 액션: 호출, 대기 취소, 입장하기(선택 팀)
- 호출 중: 「입장 대기 mm:ss」 표시
- 실시간 갱신(Socket) + 백업 폴링

#### 6.1.2 대기자 내역
- 검색: 기간 프리셋(오늘~전체), 커스텀 날짜, 상태(대기 중/완료/취소), 연락처·인원·등록번호
- 목록 선택(체크박스), `총 N / 선택 M`
- 엑셀 다운로드: 선택 건 우선, 없으면 검색 조건 전체
- 페이지네이션

#### 6.1.3 고객 관리
- 검색: 기간, 마케팅 동의/미동의, 전화번호(숫자만)
- 선택·총/선택 개수·선택 건 엑셀 다운로드
- 마케팅 동의 시 `동의 (YY.MM.DD HH:mm)` 형식 표시

#### 6.1.4 알림톡
- 탭: 집계/충전, 충전 내역
- 잔액·발송 건수·비용 요약
- 발송 내역: 일시, 템플릿명, 수신번호, 성공 여부, 충전금
- **[재발송]**: 원본과 동일 템플릿/변수로 알림톡 재전송, 결과 뱃지(성공/실패)
- 충전: NicePay 실결제(기본). `NICEPAY_ALLOW_MOCK=1`일 때만 MOCK 직접충전 허용. 최소 1,000원

#### 6.1.5 공지사항
- 시스템 관리자가 등록한 공지 열람(읽기 전용)

#### 6.1.6 설정
- 시설 정보: 시설명, 프로필 이미지, 테마, 관리자 연락처
- 대기 운영: 충전 경고 금액, 입장 임박 알림 순번, 입장 대기 분, 팀당 평균 대기, 매장 안내, 미루기 정책/횟수, 사용 언어, 웨이팅 유형
- 약관/안내 문구(다국어)
- 비밀번호 변경

---

### 6.2 시스템 관리자

#### 6.2.1 시설사 관리
- 검색: 시설명/코드, 상태(활성화/탈퇴)
- 시설 등록·수정: 코드, 마스터 비밀번호, 알림톡 단가, 광고 노출, 상태(ACTIVE/WITHDRAW)
- 사용자/관리자/사이니지 URL 복사

#### 6.2.2 공지사항 관리
- 전 시설 노출 공지 CRUD (제목, 버전, HTML 본문)

#### 6.2.3 대기자 내역 / 고객 관리 (전체)
- 시설사 필터 포함 전역 조회
- 선택·엑셀 등 시설 관리자와 유사

#### 6.2.4 알림톡
- 시설별 발송 집계, 관리자 충전, 충전 내역
- 충전 건 부분 취소(잔액 차감 + 환불 안내)

#### 6.2.5 설정
- 시스템 공통 관리자 연락처(충전/환불 알림 수신)
- 비밀번호 변경

---

### 6.3 사이니지
- 1920×1080 기준 스케일 표시
- 영역
  - **입장 호출**: 호출된 pending 팀(최신 1건). 없으면 「호출 대기 중」
  - **최근 입장 호출**: 호출 이력이 있는 완료 건(최대 5)
  - **현재 대기**: 호출 중 팀 제외 대기열(최대 7), 첫 행 「다음 호출」
- 시계(KST), 시설 로고/명칭
- 관리자 호출 시 실시간 반영

---

## 7. 대기 상태 정의

| 상태 코드 | 화면 표기(예) | 의미 |
|-----------|---------------|------|
| `pending` | 대기 중 | 등록 후 입장 전 |
| `completed` | 대기 완료 | 입장 처리 완료 |
| `cancelled` | 대기 취소 | 고객이 직접 취소 |
| `admin_cancelled` | 관리자 취소 | 시설 관리자가 취소 |
| `no_show` | 미입장 | 호출 후 시간 초과 자동 취소 |

---

## 8. 카카오 알림톡 명세

### 8.1 발송 조건
- 시설 충전금(`kakao_balance`) ≥ 알림톡 단가(`kakao_unit_cost`, 시설별 설정, 기본 20원)
- 뿌리오 미설정 시 **MOCK** 모드(실제 미발송, 이력/차감은 테스트 정책에 따라 기록)

### 8.2 템플릿·트리거

| 구분 | 트리거 | 비고 |
|------|--------|------|
| 웨이팅 등록 완료 | 고객 등록 성공 | 완료 페이지 링크 포함 |
| 입장 임박 | 대기 순번이 설정 순번에 도달 | 팀당 1회 |
| 입장 안내(호출) | 관리자 [호출] | 사이니지와 연동 |
| 미입장 취소 | 호출 후 타임아웃 | `no_show` |
| 웨이팅 취소 | 고객/관리자 수동 취소 | |
| 순서 변경(미루기) | 미루기 성공 | |
| 충전/환불 안내 | 잔액 변동 | 시설·시스템 관리자 연락처로 발송, 잔액 미차감 |

### 8.3 재발송
- 시설 관리자 알림톡 화면에서 발송 이력 기준 **[재발송]**
- 원본과 동일한 템플릿·치환 변수로 재전송
- 성공/실패 결과를 해당 행에 뱃지로 표시(새로고침 후에도 유지)

---

## 9. 실시간 반영(Socket.io)

### 9.1 목적
대기 상태 변경 시 사이니지·완료 페이지·시설 대기자 리스트를 즉시 갱신

### 9.2 범위
- 시설(`facilityCode`) 단위 room 분리 → 타 시설 이벤트 미전달
- 주요 시점: 등록 / 호출 / 완료 / 취소 / 미루기
- 재연결 시 REST로 최신 상태 재조회
- 단일 인스턴스 기준(추후 Redis Adapter로 다중 인스턴스 확장 가능)

---

## 10. 과금·잔액 규칙

| 항목 | 설명 |
|------|------|
| 단가 | 시설별 알림톡 단가 |
| 차감 | 알림톡 실발송/MOCK 성공 시(정책에 따라) 차감 |
| 충전 | 시설 관리자 NicePay / 시스템 관리자 수동 충전 |
| 경고 | 잔액 ≤ 충전 필요 알림 금액 시 관리자 화면에 충전 유도 |
| 부족 | 잔액 부족 시 알림톡 발송 실패 가능(웨이팅 등록과 분리) |
| 취소 | 시스템 관리자가 충전 건 부분 취소 시 잔액 차감 + 환불 안내 |

---

## 11. 다국어
- 지원: 한국어 / English / 日本語 / 中文
- 시설 설정에서 사용 언어 활성화
- 고객 화면 약관·안내 문구는 언어별 본문 설정 가능

---

## 12. 비기능·운영 참고 (메뉴얼 부록용)

### 12.1 환경
- 프론트엔드: React + Vite
- 백엔드: Node.js(Express) + PostgreSQL
- 외부 연동: 뿌리오 알림톡, NicePay

### 12.2 데모 접속(개발/검수용 — 실제 운영 계정과 다를 수 있음)
메뉴얼에 포함할 경우 **환경별로 확인 후** 기입하세요.
- 고객: `/w/demo-park`
- 사이니지: `/signage/demo-park`
- 시설 관리자 / 시스템 관리자: 배포 환경 계정 정책 따름

### 12.3 메뉴얼 작성 시 권장 목차
1. 제품 소개 및 역할별 시작하기  
2. 고객(키오스크) 등록 가이드  
3. 고객(모바일) 웨이팅 관리  
4. 시설 관리자 — 대기 운영(호출/입장/취소)  
5. 시설 관리자 — 내역·고객·알림톡·설정  
6. 사이니지 설치·표시 설명  
7. 시스템 관리자 — 시설/공지/과금  
8. 알림톡 종류와 발송 시점  
9. FAQ (잔액 부족, 미입장, 미루기 횟수 등)

---

## 13. 용어집

| 용어 | 설명 |
|------|------|
| facilityCode | 시설사 고유 코드. URL 경로에 사용 |
| 등록번호(dailySeq) | 당일 시설 내 대기 등록 순번 |
| 대기 순번(order) | 현재 pending 큐에서의 위치 |
| 호출 | 입장 유도. 알림톡 + 사이니지 + 타이머 |
| 입장하기 | 대기 완료 처리(호출 알림 없음) |
| 미입장 | 호출 후 제한 시간 초과 자동 취소 |
| 충전금 | 알림톡 발송에 쓰이는 시설 잔액 |
| MOCK | 알림톡 실제 미발송 테스트 모드 |

---

## 14. 범위 외 / 후속 확장(참고)
- 다중 API 인스턴스 간 Socket 동기화(Redis Adapter) — 설계 준비만 되어 있음
- 본 PRD는 **현재 구현된 기능** 중심이며, 미구현 로드맵 항목은 별도 관리

---

*문서 생성 기준: tbridgeWaiting 코드베이스 현재 구현*  
*메뉴얼 제작 시 스크린샷·실제 운영 URL·계정 정보는 배포 환경에 맞게 보완하세요.*
